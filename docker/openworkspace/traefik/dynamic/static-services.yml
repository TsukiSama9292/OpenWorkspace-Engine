http:
  services:
    api-service:
      loadBalancer:
        servers:
          - url: "http://ow-api:3000"

    web-service:
      loadBalancer:
        servers:
          - url: "http://ow-web:80"
